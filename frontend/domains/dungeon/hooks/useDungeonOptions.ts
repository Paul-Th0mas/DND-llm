"use client";

/**
 * Dungeon generation uses static form inputs (sliders, number fields).
 * No backend options endpoint exists — all bounds are defined in the domain types.
 *
 * This module is intentionally empty. Import constraints from the dungeon types
 * or declare local constants in components that need them.
 */

export {};
